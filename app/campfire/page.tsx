import type { Metadata } from "next";import { requireChatGPTUser } from "../chatgpt-auth";import { AppShell } from "../components/AppShell";import { CampfireClient } from "./campfire-client";
export const metadata:Metadata={title:"Campfire"};export const dynamic="force-dynamic";export default async function Campfire(){await requireChatGPTUser("/campfire");return <AppShell><main className="wrap interior-page"><header className="page-hero"><p className="eyebrow">Private league Tribal Council</p><h1>Bring it to the fire.</h1><p>Pitch ideas for the league, support the twists you want to play, and keep the family conversation going between episodes.</p></header><CampfireClient/></main></AppShell>}
