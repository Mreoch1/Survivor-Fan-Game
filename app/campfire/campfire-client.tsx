"use client";import { useEffect,useState } from "react";type Post={id:number;body:string;createdAt:string;name:string;teamName:string};export function CampfireClient(){const[posts,setPosts]=useState<Post[]>([]),[body,setBody]=useState(""),[busy,setBusy]=useState(false),[locked,setLocked]=useState(false),[revealAt,setRevealAt]=useState<string|null>(null),[error,setError]=useState("");const load=()=>fetch("/api/posts").then(r=>r.json()).then(d=>{setPosts(d.posts||[]);setLocked(!!d.locked);setRevealAt(d.revealAt||null)});useEffect(()=>{load()},[]);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/posts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({body})}),out=await r.json();setBusy(false);if(r.ok){setBody("");await load()}else setError(out.error||"That message could not be posted.")}return <><div className="notice"><strong>Spoiler-safe campfire</strong><span>{locked&&revealAt?`Posting reopens ${new Date(revealAt).toLocaleString("en-US",{weekday:"long",hour:"numeric",minute:"2-digit",timeZone:"America/Detroit"})} ET.`:"Please keep future-episode spoilers out of the chat."}</span></div><div className="campfire-layout"><form className="panel post-box" onSubmit={submit}><label htmlFor="post">Add to the campfire</label><textarea id="post" maxLength={500} required disabled={locked} value={body} onChange={e=>setBody(e.target.value)} placeholder={locked?"Campfire is quiet during the spoiler window.":"I’m calling a blindside this week…"}/>{error&&<p>{error}</p>}<div><small>{body.length}/500</small><button className="button button-primary" disabled={busy||locked}>{busy?"Posting…":locked?"Spoiler window":"Post message"}</button></div></form><section className="post-feed">{posts.length?posts.map(p=><article className="post" key={p.id}><div className="avatar small">{p.name[0]}</div><div><header><strong>{p.teamName||p.name}</strong><time>{new Date(p.createdAt).toLocaleString()}</time></header><p>{p.body}</p></div></article>):<div className="notice">The campfire is quiet. Start the conversation.</div>}</section></div></>}
