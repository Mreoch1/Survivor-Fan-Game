# Supabase (create project at https://supabase.com)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: public URL for link previews and auth redirects (default: https://survivor-fan-game.vercel.app)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Required for episode scoring (process-episode + Friday cron). Supabase Dashboard → Settings → API → service_role secret.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Required for Vercel Cron. Set in Vercel project env; Vercel sends it when invoking the cron. Generate: openssl rand -hex 32
# CRON_SECRET=your-cron-secret

# Optional: for Supabase CLI. Get from dashboard URL: .../project/<PROJECT_REF>
# SUPABASE_PROJECT_REF=abcdefgh
# SUPABASE_DB_PASSWORD=your-database-password

# Optional: for npm run email-templates (Management API). Get from https://supabase.com/dashboard/account/tokens
# SUPABASE_ACCESS_TOKEN=your-personal-access-token

# Optional: theme music URL (royalty-free MP3, e.g. from Pixabay)
# NEXT_PUBLIC_THEME_MUSIC_URL=https://cdn.pixabay.com/audio/...
